import { PieceDirectiveDirective } from './piece-directive.directive';

describe('PieceDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PieceDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
